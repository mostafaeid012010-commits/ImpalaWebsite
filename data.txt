⚙️ تعديل حجم اللوجو في style.css
قم بفتح ملف style.css وقم بتعديل القيم التالية:

1. تكبير الشعار في شريط الـ Header
هذا الجزء مسؤول عن حجم الشعار الذي يظهر في أعلى الموقع (بجانب قائمة التنقل). ابحث عن هذا الكود وقم بزيادة قيمة height:

CSS

/* --- HEADER & NAVIGATION --- */
/* ... الكود ... */

.logo img {
    height: 65px; /* كانت 50px - قم بزيادتها لـ 65px أو حسب ما تراه مناسباً */
    width: auto;
    filter: brightness(0) invert(1); 
}
2. تكبير اللوجوهات في شاشة التحميل (Preloader)
هذا الجزء مسؤول عن حجم اللوجوهات التي تتحرك في شاشة التحميل في بداية كل صفحة. ابحث عن هذا الكود وقم بزيادة قيمة height:

CSS

/* ========================================
   PRELOADER (شاشة التحميل)
   ======================================== */

/* ... الكود ... */

.logo-animation-container img {
    height: 90px; /* كانت 70px - قم بزيادتها لـ 90px أو حسب ما تراه مناسباً */
    width: auto;
    filter: brightness(1.5) grayscale(50%);
    transition: transform 0.3s;
}
3. تكبير الشعار في الخلفية المتحركة (Hero Section)
هذا الجزء يخص صفحة index.html (الشعار الكبير الباهت في الخلفية). ابحث عن هذا الكود وقم بزيادة قيمة background-size:

CSS

/* --- HOME PAGE: HERO SECTION --- */

.hero::before {
    /* ... الكود ... */
    background-size: 60%; /* كانت 50% - قم بزيادتها لـ 60% أو حسب ما تراه مناسباً */
    opacity: 0.05;
    z-index: 1; 
    /* ... الكود ... */
}
الخلاصة: بعد تطبيق هذه التعديلات الثلاثة في ملف style.css، سيكون لديك شعار أكبر حجماً في جميع أنحاء الموقع، مع الحفاظ على التناسق.

هل تريد مني إجراء أي تعديلات أخرى على ملفات الموقع؟





هذا تعديل ممتاز لإضافة طابع بصري مميز لموقعك!

لإضافة صورة خلفية لشاشة التحميل (Preloader)، يجب أن نعدل ملف style.css. لا نحتاج لتعديل أي ملفات HTML.

⚙️ التعديل المطلوب في ملف style.css
قم بالبحث عن الجزء الخاص بـ #pagePreloader في ملف style.css واستبدله بالكامل بالكود التالي. لقد أضفت فيه الخصائص اللازمة للصورة وطبقة تعتيم لضمان وضوح اللوجوهات المتحركة:

CSS

/* ========================================
   PRELOADER (شاشة التحميل) - تم التعديل لإضافة صورة خلفية
   ======================================== */

#pagePreloader {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999;
    opacity: 1;
    transition: opacity 1s ease-out;
    
    /* --- خصائص الصورة الخلفية --- */
    background-image: url('images/preloader-bg.jpg'); /* <<<<< هام: غيّر هذا المسار لصورتك */
    background-size: cover;
    background-position: center;
    background-color: var(--header-bg); /* لون احتياطي في حال فشل تحميل الصورة */
    position: relative; /* ضروري لطبقة التعتيم */
}

/* إضافة طبقة تعتيم (Overlay) لجعل اللوجوهات واضحة فوق الصورة */
#pagePreloader::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7); /* تعتيم أسود بنسبة 70% */
    z-index: 9990; /* أقل من اللوجوهات */
}

.logo-animation-container {
    /* ... (تأكد أن هذا الكلاس يحتوي على z-index عالي) */
    display: flex;
    gap: 40px;
    justify-content: center;
    align-items: center;
    animation: slideLogos 2.5s infinite ease-in-out; 
    position: relative;
    z-index: 9991; /* تم التأكد من أن اللوجوهات فوق طبقة التعتيم */
}

/* ... (باقي كود .logo-animation-container img) ... */
💡 ملحوظة هامة
مسار الصورة: تأكد من أنك قمت بوضع الصورة التي تريدها كخلفية في مجلد images، وأن اسم الملف هو preloader-bg.jpg (أو قم بتعديل هذا الاسم في الكود ليطابق اسم ملفك الفعلي).

بعد هذا التعديل، ستظهر شاشة التحميل بصورة خلفية أنيقة مع طبقة تعتيم خفيفة.





